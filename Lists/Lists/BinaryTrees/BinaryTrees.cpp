#include <cstdlib>
#include <iostream>
#include "BinaryTrees.h"

using namespace std;

BinaryTree::BinaryTree() {
	root = NULL;
}

void BinaryTree::AddNode(int newValue) {
	if (root != NULL) {
		if (newValue) {

		}
	}
}